os: linux
arch: arm64
dist: focal
# virt: vm
# group: edge
language: c++

env:
  - MAKEOPTS='-j2'

install:
  - sudo apt-get install -y python3-dev
script:
  - ./fetch_resource.sh
  - ./pack_other.sh
  - ./pack_boost.sh > build_boost.log
  - ./pack_llvm.sh > build_llvm.log
  - ./pack_all.sh


